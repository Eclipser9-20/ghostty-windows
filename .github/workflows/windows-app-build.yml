on:
  push:
    branches:
      - "windows-gtk-app"
  workflow_dispatch: {}

name: Windows App Build (experimental)

jobs:
  build-windows-gtk:
    runs-on: windows-latest
    timeout-minutes: 45
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup MSYS2 with GTK4/libadwaita
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          path-type: inherit
          install: >-
            mingw-w64-x86_64-gtk4
            mingw-w64-x86_64-libadwaita
            mingw-w64-x86_64-pkgconf
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-blueprint-compiler

      - name: Setup Zig
        uses: mlugg/setup-zig@v2

      - name: Alias MinGW .dll.a import libs to .a (Zig doesn't recognize .dll.a)
        run: |
          for f in /mingw64/lib/*.dll.a; do
            cp -n "$f" "${f%.dll.a}.a"
          done

      - name: Attempt GTK app-runtime build
        env:
          PKG_CONFIG_ALLOW_SYSTEM_LIBS: "1"
          PKG_CONFIG_ALLOW_SYSTEM_CFLAGS: "1"
        run: |
          pkg-config --libs gtk4
          zig build -Dapp-runtime=gtk -Dtarget=x86_64-windows-gnu
